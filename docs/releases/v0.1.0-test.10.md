# GitCurrent v0.1.0-test.10

这是 GitCurrent 的第十个 GitHub 测试版本。本版本基于 `v0.1.0-test.9` 的源码，主要
用于验证新的应用 build number、GitHub release 流程和持续集成环境；不作为正式生产版本
分发。

## 运行要求

- Apple Silicon Mac（arm64）
- macOS 14 Sonoma 或更高版本

## 本次重点

- 保留 `v0.1.0-test.9` 中的 Working Copy、History、改动文件 Diff、可调整分栏、原生
  拖拽光标、分支选中高亮和提交/分支上下文操作。
- 验证应用版本从 `0.1.0 (9)` 递增为 `0.1.0 (10)`。
- 验证源码、测试、macOS arm64 构建和 GitHub prerelease 发布链路。

本版本没有额外的功能代码变更；如需查看上一版本的完整功能说明，请参阅
`docs/releases/v0.1.0-test.9.md`。

## 兼容性

- 保留现有 Bundle ID、偏好设置 Key 和数据格式，覆盖旧测试版不会丢失最近仓库与本地
  设置。
- 构建产物为 `GitCurrent.app`，应用版本为 `0.1.0 (10)`。

## 重要限制

- 此构建未使用 Developer ID 签名，也未经过 Apple 公证。
- Sparkle 更新通道和 EdDSA 更新签名尚未启用。
- 内置 Git/Git LFS 工具链为 Apple Silicon 版本，因此本测试版仅支持 arm64 Mac。
- 本次 GitHub release 仅发布源码和测试说明，不附带打包或签名安装包；低版本兼容性由
  GitHub Actions 负责验证。
- 仅用于受控测试，不应作为正式版本分发。

首次启动时，macOS 可能阻止打开未签名应用。请在 Finder 中按住 Control 点击
`GitCurrent.app`，选择“打开”，然后确认启动；不要关闭系统安全机制。

## 验证

- Swift 完整测试：222 项通过，3 项环境相关用例按预期跳过。
- macOS arm64 Debug 构建通过。
- `git diff --check` 通过，`sites/current-macos-prototype/` 保持在忽略列表中。
